<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.OsirisNFT" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/yellow</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/yellow</item>
        <item name="colorSecondaryVariant">@color/yellow</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/black</item>
        <item name="android:colorControlHighlight">@color/yellow_transparent</item>

    </style>

    <style name="drawableButton">
        <item name="android:buttonTint">@animator/button_tint_light</item>
    </style>

    <style name="background">
        <item name="backgroundTint">@color/white</item>
    </style>

    <style name="cardBack">
        <item name="cardBackgroundColor">@color/white</item>
    </style>

    <style name="linearLayoutBack">
        <item name="android:background">@color/white</item>
    </style>

    <style name="navBarBack">
        <item name="android:background">@color/white</item>
    </style>

    <style name="toggleButtonBack">
        <item name="android:background">@drawable/toggle_selector_light</item>
    </style>

    <style name="yellowButton">
        <item name="backgroundTint">@color/yellow</item>
        <item name="cornerRadius">24dp</item>
        <item name="android:paddingLeft">12dp</item>
        <item name="android:paddingRight">12dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>